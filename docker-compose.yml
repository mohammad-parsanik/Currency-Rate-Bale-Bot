version: '3.8'

services:
  bot:
    build: .
    container_name: currency_bale_bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
